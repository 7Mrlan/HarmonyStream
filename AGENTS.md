# AGENTS

> 本文件是 agent 行为唯一真源。Trae / Claude Code / Cursor 等 agent 都以本文件为准；不再保留 `CLAUDE.md` 或其它私域规则文件。

- 使用 typescript 编写代码
- 不改无关逻辑、结构和文件。
- 组件式开发，优先做可复用的必要组件，写页面优先复用项目已封装组件

# 语言

- 和我对话的语言默认中文

# 注意

默认情况下，不要创建任何新的说明文档或文档文件。
不要自动生成 README.md、设计文档、使用说明、架构说明等。
只有在我明确要求"编写文档 / 生成 README / 写说明文档"时，才允许创建或修改文档。
- 所有操作步骤必须先标明执行载体（`终端命令` / `VSCode 命令面板` / `编辑器界面操作` / `浏览器操作`）；不能在 shell 执行的 IDE 动作禁止写成命令行形式，同时存在两种做法时必须分别标注。
- 给用户的命令必须是当前环境真实可运行的；如果本质是菜单路径或快捷键，用步骤描述，不能伪装成命令。

# 缓存陷阱（高频）

`packages/*` 源码改动后，`apps/mobile` 跑出来仍是旧版 = 默认是 Metro / 浏览器缓存，不是代码问题。

- 用户反馈「代码改了但页面没变」时：先核对源码链路（`import` 路径 + 子包 `package.json main`）；链路 OK 立刻判定为缓存问题，不要再改代码。
- 修复三步，必须分别标注执行载体：
  1. 终端：在跑 `expo start` 的窗口按 `Ctrl + C` 停掉 Metro。
  2. 终端命令：`pnpm dev -- --clear`（透传给 Expo CLI 清 transformer cache）。
  3. 浏览器操作：`Ctrl + Shift + R` 硬刷新（F5 会复用 `index.bundle` 的 disk cache，不算数）。
- 三步走完仍未生效，再排查 Metro `watchFolders`、pnpm hoist、组件实际渲染链路。
- 详细案例与历史踩坑见 `tasks/lessons.md`。

# 代码规范

- 代码要写清楚中文注释，所有函数和关键逻辑都必须有注释，注释永远使用多行注释 `/* */`

---

# Workflow Orchestration

## 1. 渐进式 Spec：按需复杂度

不同复杂度的需求，走不同深度的流程，偶然复杂度应该尽可能压缩：

| 需求规模 | 流程 |
|---------|------|
| 简单（改字段、修 bug） | 直接执行，无需 Spec |
| 中等（3+ 步骤，有架构决策） | 写轻量 Spec，HARD-GATE 后再编码 |
| 复杂（跨模块、多系统） | 完整 Propose → Apply → Review |

**Spec 三铁律**（仅中等及以上复杂度触发）：
1. **No Spec, No Code** — 没有 Spec，不准写代码
2. **Spec is Truth** — Spec 和代码冲突时，错的一定是代码
3. **Reverse Sync** — 执行中发现偏差，先修 Spec，再修代码

**HARD-GATE**：Spec 完整生成后，必须等用户显式确认才能开始编码。确认前禁止任何代码修改动作。

**Research 必须有出处**：描述代码现状时，每个结论必须标注文件路径 + 函数名，不接受“通常来说”或无依据的推断。

**Spec 分段确认**：不一口气生成完整 Spec。按段输出（现状分析 → 功能点 → 风险与决策），每段等用户确认后再继续。越早发现方向偏差，修正成本越低。

## 2. Plan Node Default

- 对任何中等及以上复杂度的任务，进入 plan mode
- 出问题立刻停下重新规划，不要强行推进
- Plan mode 同样适用于验证步骤，不只是构建阶段

## 3. Subagent Strategy

- 大量使用 subagent 保持主 context 窗口干净
- Research、探索、并行分析交给 subagent
- 复杂问题通过 subagent 投入更多计算
- 每个 subagent 只做一件事，专注执行

## 4. 执行自由度曲线

| 阶段 | 自由度 | 原则 |
|------|--------|------|
| 调研 | 中 | 自由探索，但结论必须有代码出处 |
| 方案设计 | 高 | 充分想象，提选项 + 给推荐 |
| 规划 | 低 | 精确到文件路径和函数签名 |
| 执行 | 零 | 严格按计划，有偏差立即停下问 |
| 验收 | 中 | 自由检查，结论要有依据 |

## 5. Goal-Driven Execution

把任务转写成可验证目标，再循环到验证通过；强目标可独立执行，弱目标（"让它能跑"）需要反复澄清。

- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

多步任务先给简短 plan：

```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

## 6. Self-Improvement Loop

- 用户每次纠正后：将模式写入 `tasks/lessons.md`
- 写规则防止同类错误重现
- 每次会话开始时 review lessons 里的相关规则
- 有价值的踩坑和领域发现，主动建议沉淀到项目知识库

## 7. Verification 铁律

- 任务未经验证，不得标记为完成
- 必须展示可验证的证据（编译输出 / 测试结果 / 运行日志）
- 禁止"应该没问题"等无证据声明
- 必要时对比修改前后的行为差异
- 执行构建命令时，先按"实测耗时 + 60 秒"设置工具超时，不要在短超时后直接重复构建。

## 8. Demand Elegance（适度）

- 非简单修改时，停下来问一句："有没有更优雅的方式？"
- 如果方案感觉 hacky："知道了这些之后，实现优雅方案"
- 简单显而易见的修复直接做，不要过度设计

## 9. Autonomous Bug Fixing

- 给 bug 报告就去修，不要等手把手指导
- 指向日志、报错、失败测试，然后解决它
- 不需要用户切换上下文
- CI 测试失败，主动去修

## 10. Surgical Changes

只动你必须动的；只清你自己造成的烂摊子。

- 不要"顺手优化"邻近代码、注释、格式。
- 不要重构没坏的代码。
- 匹配现有风格，即使你会用别的写法。
- 注意到不相关的死代码，提一句即可，不要删。
- 你的改动让某些 import / 变量 / 函数变成孤儿时，删掉它们。
- 不要删除 pre-existing 的死代码，除非用户要求。

测试：每一行改动都能直接追溯到用户请求。

## 11. 下一步决策规则

中等及以上任务完成后，如需推荐下一步：

- 给出 1 句话理由（主线价值 + 可行性），标注 spec 或代码出处
- 路径唯一或显而易见时，直接执行，不需要写理由
- 不接受无依据的"感觉应该做 X"

---

# Task Management

1. **先写计划**：将计划写入 `tasks/todo.md`，使用可勾选的任务项
2. **确认后执行**：中等及以上复杂度任务，HARD-GATE 后才开始实现
3. **追踪进度**：完成一项立刻标记
4. **解释变更**：每步给出高层次说明
5. **记录结果**：在 `tasks/todo.md` 末尾添加 review 小节
6. **沉淀教训**：用户纠正后更新 `tasks/lessons.md`

## Doc Lifecycle 规则（防止文档膨胀）

`tasks/todo.md` 与 `tasks/spec.md` 是当前真源，必须显式管理生命周期，不允许无限累加。

### 阶段完成后的 Spec 归档

- 文档压缩以阶段生命周期为触发器，不以行数作为主要触发器。
- 阶段验收通过后（typecheck + 烟测全绿），必须立刻把该阶段完整 Spec 存档到 `tasks/spec/<phase-name>.md`。
- `tasks/spec.md` 只保留：跨阶段有效决策、当前活跃阶段完整内容、已完成阶段摘要。
- 已完成阶段在 `tasks/spec.md` 保留 3-5 行摘要：做了什么、关键决策、遗留风险、归档链接。
- `tasks/spec.md` 超过 250 行时，不强制压缩当前阶段；只检查是否存在已完成但未归档的阶段。
- `tasks/todo.md` 超过 120 行时，先压缩已完成任务，再追加新的 Review 或下一阶段任务。

### `tasks/todo.md`

- 进行中阶段：完整保留 Spec 状态、执行任务、验证清单。
- 阶段 Review 完成、且进入下一阶段时：把上一阶段压缩成 1 行摘要并入 "已完成摘要" 表。
- 永远保留：当前主线、当前规则、最新验收提醒、当前阶段全量内容。
- 软上限：单文件 120 行。超过先压缩历史阶段，再考虑加新内容。

### `tasks/spec.md`

- Spec 写入时原文保留，允许长。
- 目标结构：跨阶段有效决策、当前阶段完整 Spec、已完成阶段摘要。
- 阶段验收通过后，必须执行一次 "decisions still active" 扫描：
  - 仍约束未来代码的决策（如 provider chain、LX 隔离、TTS ducking）→ 保留并提升到跨阶段有效决策区。
  - 已完成阶段完整正文 → 移到 `tasks/spec/<phase-name>.md`。
  - 仅描述当时背景或一次性选择的决策 → 在已完成阶段摘要中保留 3-5 行。
- `tasks/spec.md` 超过 250 行时，只检查是否有已完成阶段未归档；当前活跃阶段不因行数被压缩。

### `tasks/lessons.md`

- 用户纠正 / 复发踩坑才追加；不允许把每次心得都记进来。
- 一年以上未触发的规则可以归档到 `tasks/lessons.archive.md`（仍保留可检索）。

---

# Core Principles

- **Simplicity First**：每次改动尽量简单。最小化影响范围。
- **No Laziness**：找根因，不打补丁，用 senior developer 标准。
- **Minimal Impact**：只改必要的代码，避免引入新问题。
- **意图分离**：一次只处理一种意图，探索、决策、执行、审查不要混着来。
- **执行上下文清晰**：所有“让用户去做”的步骤都要说明执行位置，避免把编辑器动作、系统操作和终端命令混写。
